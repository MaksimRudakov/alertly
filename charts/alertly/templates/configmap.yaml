apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "alertly.fullname" . }}
  labels:
    {{- include "alertly.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}
